
<%#= @bill_bulk_data.each_pair { |key, value| hash[key] = value.to_a } %>

<%# new_hash = @bill_bulk_data.results.inject({}) {|h, (key, value)| h[key] = value; h } %>

<%# newer_hash = new_hash.inject({}) {|h, (key, value)| h[key] = value; h }%>

<%# newest_hash = newer_hash.inject({}) {|h, (key, value)| h[key] = value; h } %>   
<%#= newest_hash %>

<%# first = @bill_bulk_data.each{|k,v| @bill_bulk_data[k]=*v} %>

<%# second  = first.collect { |k, v| v } %>

<%#= second.flatten %>

<%#= @response %>

<%#= xml_doc = Nokogiri::XML(@response) %>




<%# xml_doc = Nokogiri::XML('@response') %>
<%# unpack = @response.values_at('result_set') %>
<%# unpacked = [] %>
<%# further_unpacked = [] %>
<%# n = @response.length %>

<%# count=0 %>
<%# while count <= n do %>
<%# unpacked << @response.at(n) %>
<%# count = count + 1 %>
<%# end %>

<%# further_unpacked << unpacked.fetch(0) %>

<%=  @response %>

